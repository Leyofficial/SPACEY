import style from './WishList.module.scss'

const WishList = () => {
    return (
        <article className={style.wishList}>
            wish list
        </article>
    );
};

export default WishList;